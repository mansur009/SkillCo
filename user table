-- Users Table
CREATE TABLE users (
    id SERIAL PRIMARY KEY,
    email VARCHAR(100) UNIQUE NOT NULL,
    password_hash TEXT NOT NULL,
    role VARCHAR(20) CHECK (role IN ('homeowner', 'tradesperson', 'admin')),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);